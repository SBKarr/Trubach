
include templates/include/nav.pug

doctype html
html
	head
		title Trubach
		+style
	body
		+nav
		.main
			h2 Upload list
			form.update(
				method="POST",
				enctype="multipart/form-data",
				action="/admin/importChannels?target=" + encodeURIComponent(window.location.href)
			)
				p
					input(name="content",type="file")
					input(type="submit",value="Upload")

			h2 actions
				p.links
					a.action.update(href="/admin/updateStats?target=" + encodeURIComponent(window.location.href)) Обновить статистику
